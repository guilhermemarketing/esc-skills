# ──────────────────────────────────────────────────────────────
# As pastas skills/ e workflows/ são ESPELHOS de .agent/skills/
# e .agent/workflows/ usados SOMENTE para publicar no GitHub.
# Não edite diretamente — as versões reais ficam em .agent/.
# Para publicar: copiar de .agent/ → raiz e fazer
# git add -f skills/ workflows/ && git commit && git push
# ──────────────────────────────────────────────────────────────
skills/
workflows/

# Credenciais e chaves
keys/

# Diretórios internos do agente (não fazem parte do repo público)
.agent/skills/esc-skills-deploy/
.agent/skills/esc-skills-showcase/
.agent/workflows/

# Docs internos
docs/

# Arquivos do site (gerados pelo build)
/index.html
/skills-data.js
site/skills-data-v*.js

# OS
.DS_Store
